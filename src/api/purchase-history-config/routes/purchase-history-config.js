'use strict';

/**
 * purchase-history-config router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::purchase-history-config.purchase-history-config');
